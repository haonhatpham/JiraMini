{
  "name": "jiramini",
  "private": true,
  "scripts": {
    "dev:fe": "npm --prefix fe run dev",
    "dev:be": "npm --prefix be run dev",
    "build:shared": "npm --prefix shared run build",
    "build:fe": "npm --prefix fe run build",
    "build:be": "npm --prefix be run build",
    "build:all": "npm run build:shared && npm run build:fe && npm run build:be",
    "lint:fe": "npm --prefix fe run lint",
    "lint:be": "npm --prefix be run lint",
    "prettier:shared": "npm --prefix shared run prettier",
    "prettier:fe": "npm --prefix fe run prettier",
    "prettier:be": "npm --prefix be run prettier",
    "install:shared": "npm --prefix shared install",
    "install:fe": "npm --prefix fe install",
    "install:be": "npm --prefix be install",
    "install:all": "npm --prefix shared install && npm --prefix fe install && npm --prefix be install"

  },
  "dependencies": {
    "swagger-ui-express": "^5.0.1",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "@types/swagger-ui-express": "^4.1.8",
    "@types/yamljs": "^0.2.34"
  }
}
